<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @message.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @message.content %>
</p>

<%= link_to 'Edit', edit_message_path(@message) %> |
<%= link_to 'Archive', @message, method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Back', messages_path %>
